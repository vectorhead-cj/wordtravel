/**
 * Paste a notation string here, then from the WordTravel folder run:
 *
 *   npm run puzzle:lab
 *
 * Multi-line is fine inside the backticks. Uppercase letters = givens, lowercase = player fills
 * (same as debug panel output from serializeGridDebug).
 *
 * One-line puzzles can instead use:
 *   WT_NOTATION='^.~.' npm run puzzle:lab
 */

export const PUZZLE_NOTATION = `R̈AGẼ 
ölẽ  
 Ẽŷẽ̋ 
 T..^
 ^^^.
 ... 
 I̒RŐN`;
